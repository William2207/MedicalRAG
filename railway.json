{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "/bin/sh -c 'lightrag-server --host 0.0.0.0 --port ${PORT:-9621}'",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
